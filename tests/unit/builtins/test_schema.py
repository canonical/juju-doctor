def test_load_schema():
    pass
    # GIVEN a schema file
    # WHEN the contents are read
    # THEN there are no errors

# TODO parametrize each builtin here with an instance. For each Builtin.is_schema_valid
def test_schema_validation():
    pass
    # FIXME Load tests/resources/probes/ruleset/builtins.yaml and assert it against each Builtin class' schema
    # GIVEN a Builtin with a schema
    # WHEN the builtin assertions in a Ruleset is incorrectly formatted
    # THEN a schema validation error is raises

