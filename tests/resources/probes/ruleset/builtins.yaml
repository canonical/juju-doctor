name: A RuleSet which uses native Juju Doctor probes 
applications:
  - name: alertmanager
  - name: prometheus
    minimum: 1
    maximum: 4
relations:
  - provides: ...
    requires: ...
offers:
  - prometheus:something
  - loki:something
consumes:
  - mysql:something
# probes:
#   - something