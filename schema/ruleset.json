{
  "$defs": {
    "BaseBuiltin": {
      "description": "Base class for all Pydantic models of builtins and their validations.",
      "properties": {
        "assertions": {
          "items": {
            "$ref": "#/$defs/BaseModel"
          },
          "title": "Assertions",
          "type": "array"
        }
      },
      "required": [
        "assertions"
      ],
      "title": "BaseBuiltin",
      "type": "object"
    },
    "BaseModel": {
      "properties": {},
      "title": "BaseModel",
      "type": "object"
    },
    "ProbeAssertion": {
      "additionalProperties": false,
      "description": "Schema for a builtin Probe definition in a RuleSet.",
      "properties": {
        "name": {
          "title": "Name",
          "type": "string"
        },
        "type": {
          "title": "Type",
          "type": "string"
        },
        "url": {
          "title": "Url",
          "type": "string"
        },
        "with": {
          "anyOf": [
            {
              "additionalProperties": true,
              "type": "object"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "With"
        }
      },
      "required": [
        "name",
        "type",
        "url"
      ],
      "title": "ProbeAssertion",
      "type": "object"
    }
  },
  "additionalProperties": false,
  "description": "A pydantic model of a declarative YAML RuleSet.",
  "properties": {
    "name": {
      "title": "Name",
      "type": "string"
    },
    "probes": {
      "anyOf": [
        {
          "items": {
            "$ref": "#/$defs/ProbeAssertion"
          },
          "type": "array"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Probes"
    },
    "builtins": {
      "additionalProperties": {
        "$ref": "#/$defs/BaseBuiltin"
      },
      "default": null,
      "title": "Builtins",
      "type": "object"
    }
  },
  "required": [
    "name"
  ],
  "title": "RuleSetModel",
  "type": "object"
}